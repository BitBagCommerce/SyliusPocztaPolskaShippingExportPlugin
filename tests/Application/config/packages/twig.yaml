twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

services:
    _defaults:
        public: false
        autowire: false
        autoconfigure: true

    Twig\Extra\Intl\IntlExtension: ~
